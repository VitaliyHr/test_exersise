export const { SALT } = process.env;
export const { MONGODB_URI } = process.env;
export const { SESSION_SECRET } = process.env;
export const { LOGIN } = process.env;
export const { PASS } = process.env;
export const { SITE_URI } = process.env;
export const { PORT } = process.env;
